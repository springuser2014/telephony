package telephony.core;

/**
 * @todo Review after refactor, check if necessary
 */
public class MySecurityManagerService {
    public MyPrincipal findMyPrincipalByUsername(String username) {
        MyPrincipal p = new MyPrincipal();
        return p;
    }
}
